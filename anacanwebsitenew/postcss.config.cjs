/**
 * Local PostCSS config — intentionally empty.
 * Without this file, Vite walks up and picks the parent app's
 * postcss.config.js (Tailwind), which does not apply to this site.
 */
module.exports = { plugins: [] };
